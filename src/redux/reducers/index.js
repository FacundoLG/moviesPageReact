const reducers = (state, action) => {
    switch (action.type) {
        case 'GET_MOVIES':
            return {

            }
        default:
            return state
    }
}

export default reducers